# Upload Server Guide 📸

## วิธีใช้งาน

### 1. เริ่มต้น Server
เปิด PowerShell/Terminal ไปที่โฟล์เดอร์ valentine แล้วรันคำสั่ง:

```bash
python upload_server.py
```

### 2. ดู Website
- เปิด browser ไปที่: **http://127.0.0.1:8000**
- Website จะเปิดหน้า index.html โดยอัตโนมัติ

### 3. อัพโหลดรูป
- คลิกที่ช่องรูปใดๆ ใน "สิ่งที่ฉันรักเกี่ยวกับคุณ" หรือ gallery
- เลือกรูปจากเครื่องของคุณ
- รูปจะส่งไปยัง server และบันทึกใน folder `img/`

### 4. ลบรูป
- Hover ที่รูป → คลิกปุ่ม ✕
- รูปจะถูกลบจาก folder `img/` ทันที

### โครงสร้างไฟล์
```
valentine/
├── index.html                 (Website)
├── upload_server.py           (Python server)
├── UPLOAD_SERVER_GUIDE.md     (Guide นี้)
└── img/                       (Folder ที่เก็บรูป)
    ├── gallery_0.png
    ├── gallery_1.png
    ├── fact_0.png
    ├── fact_1.png
    └── ...
```

## ข้อมูลเพิ่มเติม 💡

- **ไฟล์รูป** จะถูกเก็บลงในโฟล์เดอร์ `img/` โดยตั้งชื่อตาม index
  - Gallery: `gallery_0.png` ถึง `gallery_5.png`
  - Fact: `fact_0.png` ถึง `fact_5.png`

- **รูปจะคงอยู่ตลอด** แม้ปิด browser แล้วเปิดใหม่ เพราะเก็บในไฟล์จริง

- **Server port** คือ 8000 → สามารถเปลี่ยนได้ในไฟล์ `upload_server.py` บรรทัดที่มี `PORT = 8000`

## Troubleshooting 🔧

#### ❌ "Cannot upload image. Please run upload_server.py first!"
- **แก้ไข**: ตรวจสอบว่าเปิด server แล้วหรือไม่
  - รันคำสั่ง: `python upload_server.py`

#### ❌ Port 8000 ถูก occupy
- **แก้ไข**: เปลี่ยน PORT ในไฟล์ `upload_server.py`
  - เปลี่ยนจาก `PORT = 8000` เป็น `PORT = 8001` เป็นต้น

#### ❌ รูปไม่ปรากฏหลังจากอัพโหลด
- **แก้ไข**: 
  1. ตรวจสอบว่า folder `img/` มีไฟล์ไหม (เปิด File Explorer)
  2. Refresh browser (F5 หรือ Ctrl+R)
  3. เช็ค console (F12) ว่ามี error ไหม
